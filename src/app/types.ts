export interface IEnsemblAssemblies {
  common_name: string;
  ensembl_url: string;
  example_chromosome: any;
}

export interface IPackageJson {
  name: string;
  version: string;
  main: string;
  type: string;
}
